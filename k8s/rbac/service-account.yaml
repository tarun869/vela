apiVersion: v1
kind: ServiceAccount
metadata:
  name: vela
  namespace: vela
  labels:
    app.kubernetes.io/name: vela
    app.kubernetes.io/part-of: vela-vpp
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/vela-service-role
automountServiceAccountToken: true
