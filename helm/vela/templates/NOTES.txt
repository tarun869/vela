Thank you for installing {{ .Chart.Name }} v{{ .Chart.AppVersion }}!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

=== Services Deployed ===
{{- if .Values.api.enabled }}
  API:         http://{{ include "vela.fullname" . }}-api.{{ .Release.Namespace }}.svc.cluster.local
               Replicas: {{ .Values.api.replicaCount }} (max: {{ .Values.api.autoscaling.maxReplicas }})
{{- end }}
{{- if .Values.worker.enabled }}
  Worker:      Celery workers running ({{ .Values.worker.replicaCount }} replicas)
{{- end }}
{{- if .Values.dashboard.enabled }}
  Dashboard:   http://{{ include "vela.fullname" . }}-dashboard.{{ .Release.Namespace }}.svc.cluster.local
{{- end }}

=== External Access ===
{{- if .Values.ingress.enabled }}
  API:         https://{{ .Values.ingress.hosts.api }}
  Dashboard:   https://{{ .Values.ingress.hosts.dashboard }}
{{- else }}
  Ingress is disabled. Use port-forward to access services:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "vela.fullname" . }}-api 8000:80
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "vela.fullname" . }}-dashboard 8501:80
{{- end }}

=== Monitoring ===
{{- if .Values.prometheus.enabled }}
  Prometheus:  kubectl port-forward -n {{ .Release.Namespace }} svc/prometheus 9090:9090
{{- end }}
{{- if .Values.grafana.enabled }}
  Grafana:     kubectl port-forward -n {{ .Release.Namespace }} svc/grafana 3000:3000
{{- end }}

=== Next Steps ===
1. Verify pods are running:
   kubectl get pods -n {{ .Release.Namespace }}

2. Run database migrations:
   kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "vela.fullname" . }}-api -- alembic upgrade head

3. Check dispatch optimizer health:
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "vela.fullname" . }}-orchestrator --tail=50

4. View API docs:
   https://{{ .Values.ingress.hosts.api }}/docs

For support, see https://docs.vela.energy or file an issue at https://github.com/vela-energy/vela
